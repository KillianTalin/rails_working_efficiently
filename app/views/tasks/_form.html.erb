<div class="container">
  <div class="col-sm-4 col-sm-offset-4">
    <%= simple_form_for @task do |f| %>
      <%= f.input :name, label: 'Task', placeholder: 'Finir mon buisiness plan' %>
      <%= f.association :project, placeholder: 'Préparer mon tour du monde' %>
      <%= f.input :forecast_duration, placeholder: 'En combien de temps' %>
      <%= f.input :category, placeholder: 'Vie pro'%>
      <%= f.association :tag, placeholder: 'choice your tag' %>
      <div class="text-center">
        <%= f.button :submit %>
      </div>
    <% end %>
  </div>
</div>

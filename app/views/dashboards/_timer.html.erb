<div class="text-black timer-button-inline">
  <div id="clockdiv-<%= task.id %>" class="clockdiv">
<!--     <div>
      <span class="hours"></span>
    </div> -->
    <div class="timer-finis">
      <span class="minutes"><%= task.minutes.to_s.rjust(2, "0") if task.elapsed_time %></span>
    </div>
    <div>
      :
    </div>
    <div>
      <span class="seconds"><%= task.minus_seconds.to_s.rjust(2, "0") if task.elapsed_time %></span>
    </div>
  </div>
  <input id="current_elapsed_time-<%= task.id %>" type="text" style="display: none;" value="<%= task.elapsed_time %>">

  <%= simple_form_for [project, task], remote: true do |f| %>
    <%= f.input :elapsed_time, as: :hidden, input_html:{id: "elapsed_time_pause_#{task.id}"} %>
    <%= button_tag type: 'submit', class: "btn ma-btn-2 hidden", :style => "padding: 0 8px 0 5px;", id: "button-pause-#{task.id}" do %>
      <i class="fa fa-3x fa-pause-circle-o button-dashboard-padding" aria-hidden="true"></i>
    <% end %>
  <% end %>


  <div class="timer_btn">
    <i class="fa fa-play-circle js-start" id="start-start-<%= task.id %>"></i>
  </div>

  <div class="hidden" id="button-restart-<%= task.id %>">
    <i class="fa fa-3x fa-play-circle button-dashboard-padding" aria-hidden="true" style=" font-size: 40px; padding: 0 12px 0 5px;"></i>
  </div>

  <%= simple_form_for [project, task] do |f| %>
    <%= f.input :elapsed_time, as: :hidden, input_html:{id: "elapsed_time_#{task.id}"}%>
    <%= f.input :done, as: :hidden, input_html: {value: true} %>
    <div class="hidden" id="button-finish-<%= task.id %>">
      <%= button_tag type: 'submit' , class: "btn ma-btn-2" do %>
        <i class="fa fa-3x fa-check-circle-o" aria-hidden="true"></i>
      <% end %>
    </div>
  <% end %>
</div>
